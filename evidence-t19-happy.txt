T19 happy evidence (local, no production claim)

Scenario: controlled admin bootstrap
Command: npm run test:esm -- src/database/admin-bootstrap.spec.ts
Observed: first run returns created, uses PasswordService.hash, and a second run returns already_exists without a second insert or rehash. Existing non-admin email is rejected.

Scenario: environment separation
Command: npm run test:esm -- src/config/env.validation.spec.ts
Observed: local, staging, and production APP_ENV values are accepted; R2 endpoint and D1/R2 conditional contracts remain validated.

Scenario: real local seed and idempotency dry run
Command: temporary local SQLite + npm run seed:admin twice + parameterized SELECT + remove temporary local.db files
Observed: first command printed `Admin bootstrap created.`; second printed `Admin bootstrap already_exists.`; database row had role `admin` and an Argon2 prefix. No credential value was printed, and temporary database files were removed.

Scenario: rollback procedure dry run
Command: review docs/production-operations.md steps 1-8 against the local migration runner behavior
Observed: procedure requires immutable deployment revert, pre-migration backup restore or reviewed compensating migration, never edits applied migrations, and explicitly records that this is not production verification.

Regression checks:
Commands: npm run test:esm; npm run test:e2e
Observed: 36 unit suites / 313 tests passed and 1 e2e suite / 83 tests passed. No production or staging credentials were used.
